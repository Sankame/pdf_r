# PDF回転スクリプト

このリポジトリには、PDFファイルを指定した角度で回転させるためのPythonスクリプト `rotate_pdf.py` が含まれています。

## 使用方法

1. このリポジトリをクローンします。
2. 必要なパッケージをインストールします。主に`PyPDF2`が必要です。
3. `rotate_pdf.py` を実行し、回転させたいPDFファイルと角度を指定して実行します。

### 依存パッケージのインストール
